
<div>
  <%if artist.avatar.attached?%>
    <%= image_tag artist.avatar.variant(resize: "400x400"), alt: "avatar", class: "rounded-circle"%>  
  <%else%>
    <p>=== Il n'y a pas encore d'avatar lié à cet utilisateur ===</p>
  <%end%>
</div>